services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ahdah
    ports:
      - "5432:5432"
    volumes:
      - ahdah_postgres:/var/lib/postgresql/data

volumes:
  ahdah_postgres:
